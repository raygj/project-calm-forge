# ============================================================
# GENERATED BY: CALM Forge v0.2
# EDA rulebook for digital-banking-platform
# DO NOT EDIT — regenerate from CALM source
# ============================================================
- name: digital-banking-platform Day 2 Operations
  hosts: all
  sources:
  - ansible.eda.webhook:
      host: 0.0.0.0
      port: 5000
  rules:
  - name: api-gateway config drift remediation
    condition: event.payload.alert_type == "configuration_drift" and event.payload.entity_id
      == "api-gateway"
    action:
      run_job_template:
        name: day2-drift-remediation
        organization: UHCCP
        job_args:
          extra_vars:
            target_service: api-gateway
            application: digital-banking-platform
  - name: api-gateway agent health restore
    condition: event.payload.alert_type == "agent_unhealthy" and event.payload.entity_id
      == "api-gateway"
    action:
      run_job_template:
        name: day2-agent-restore
        organization: UHCCP
        job_args:
          extra_vars:
            target_service: api-gateway
            application: digital-banking-platform
  - name: account-service config drift remediation
    condition: event.payload.alert_type == "configuration_drift" and event.payload.entity_id
      == "account-service"
    action:
      run_job_template:
        name: day2-drift-remediation
        organization: UHCCP
        job_args:
          extra_vars:
            target_service: account-service
            application: digital-banking-platform
  - name: account-service agent health restore
    condition: event.payload.alert_type == "agent_unhealthy" and event.payload.entity_id
      == "account-service"
    action:
      run_job_template:
        name: day2-agent-restore
        organization: UHCCP
        job_args:
          extra_vars:
            target_service: account-service
            application: digital-banking-platform
  - name: payment-service config drift remediation
    condition: event.payload.alert_type == "configuration_drift" and event.payload.entity_id
      == "payment-service"
    action:
      run_job_template:
        name: day2-drift-remediation
        organization: UHCCP
        job_args:
          extra_vars:
            target_service: payment-service
            application: digital-banking-platform
  - name: payment-service agent health restore
    condition: event.payload.alert_type == "agent_unhealthy" and event.payload.entity_id
      == "payment-service"
    action:
      run_job_template:
        name: day2-agent-restore
        organization: UHCCP
        job_args:
          extra_vars:
            target_service: payment-service
            application: digital-banking-platform
  - name: notification-service config drift remediation
    condition: event.payload.alert_type == "configuration_drift" and event.payload.entity_id
      == "notification-service"
    action:
      run_job_template:
        name: day2-drift-remediation
        organization: UHCCP
        job_args:
          extra_vars:
            target_service: notification-service
            application: digital-banking-platform
  - name: notification-service agent health restore
    condition: event.payload.alert_type == "agent_unhealthy" and event.payload.entity_id
      == "notification-service"
    action:
      run_job_template:
        name: day2-agent-restore
        organization: UHCCP
        job_args:
          extra_vars:
            target_service: notification-service
            application: digital-banking-platform
  - name: fraud-detection config drift remediation
    condition: event.payload.alert_type == "configuration_drift" and event.payload.entity_id
      == "fraud-detection"
    action:
      run_job_template:
        name: day2-drift-remediation
        organization: UHCCP
        job_args:
          extra_vars:
            target_service: fraud-detection
            application: digital-banking-platform
  - name: fraud-detection agent health restore
    condition: event.payload.alert_type == "agent_unhealthy" and event.payload.entity_id
      == "fraud-detection"
    action:
      run_job_template:
        name: day2-agent-restore
        organization: UHCCP
        job_args:
          extra_vars:
            target_service: fraud-detection
            application: digital-banking-platform
  - name: auth-service config drift remediation
    condition: event.payload.alert_type == "configuration_drift" and event.payload.entity_id
      == "auth-service"
    action:
      run_job_template:
        name: day2-drift-remediation
        organization: UHCCP
        job_args:
          extra_vars:
            target_service: auth-service
            application: digital-banking-platform
  - name: auth-service agent health restore
    condition: event.payload.alert_type == "agent_unhealthy" and event.payload.entity_id
      == "auth-service"
    action:
      run_job_template:
        name: day2-agent-restore
        organization: UHCCP
        job_args:
          extra_vars:
            target_service: auth-service
            application: digital-banking-platform
  - name: postgres-accounts connection pool monitoring
    condition: event.payload.alert_type == "connection_pool_exhaustion" and event.payload.entity_id
      == "postgres-accounts"
    action:
      run_job_template:
        name: day2-db-connection-remediation
        organization: UHCCP
        job_args:
          extra_vars:
            target_database: postgres-accounts
            application: digital-banking-platform
  - name: postgres-payments connection pool monitoring
    condition: event.payload.alert_type == "connection_pool_exhaustion" and event.payload.entity_id
      == "postgres-payments"
    action:
      run_job_template:
        name: day2-db-connection-remediation
        organization: UHCCP
        job_args:
          extra_vars:
            target_database: postgres-payments
            application: digital-banking-platform
  - name: Certificate auto-renewal (api-gateway ↔ account-service mTLS)
    condition: event.payload.source == "vault" and event.payload.event_type == "cert_expiring"
      and event.payload.cert_path starts_with "pki/digital-banking-platform"
    action:
      run_job_template:
        name: cert-rotation-vault-pki
        organization: UHCCP
        job_args:
          extra_vars:
            pki_mount: pki/digital-banking-platform
            application: digital-banking-platform
  - name: Certificate auto-renewal (api-gateway ↔ payment-service mTLS)
    condition: event.payload.source == "vault" and event.payload.event_type == "cert_expiring"
      and event.payload.cert_path starts_with "pki/digital-banking-platform"
    action:
      run_job_template:
        name: cert-rotation-vault-pki
        organization: UHCCP
        job_args:
          extra_vars:
            pki_mount: pki/digital-banking-platform
            application: digital-banking-platform
  - name: Certificate auto-renewal (api-gateway ↔ auth-service mTLS)
    condition: event.payload.source == "vault" and event.payload.event_type == "cert_expiring"
      and event.payload.cert_path starts_with "pki/digital-banking-platform"
    action:
      run_job_template:
        name: cert-rotation-vault-pki
        organization: UHCCP
        job_args:
          extra_vars:
            pki_mount: pki/digital-banking-platform
            application: digital-banking-platform
  - name: Credential rotation monitoring (account-service → postgres-accounts)
    condition: event.payload.source == "vault" and event.payload.event_type == "lease_expiring"
      and event.payload.mount_path == "database/digital-banking-platform"
    action:
      run_job_template:
        name: credential-rotation-monitor
        organization: UHCCP
        job_args:
          extra_vars:
            db_mount: database/digital-banking-platform
            consumer_service: account-service
            application: digital-banking-platform
  - name: Credential rotation monitoring (payment-service → postgres-payments)
    condition: event.payload.source == "vault" and event.payload.event_type == "lease_expiring"
      and event.payload.mount_path == "database/digital-banking-platform"
    action:
      run_job_template:
        name: credential-rotation-monitor
        organization: UHCCP
        job_args:
          extra_vars:
            db_mount: database/digital-banking-platform
            consumer_service: payment-service
            application: digital-banking-platform
  - name: Certificate auto-renewal (payment-service ↔ fraud-detection mTLS)
    condition: event.payload.source == "vault" and event.payload.event_type == "cert_expiring"
      and event.payload.cert_path starts_with "pki/digital-banking-platform"
    action:
      run_job_template:
        name: cert-rotation-vault-pki
        organization: UHCCP
        job_args:
          extra_vars:
            pki_mount: pki/digital-banking-platform
            application: digital-banking-platform
  - name: Certificate auto-renewal (payment-service ↔ notification-service mTLS)
    condition: event.payload.source == "vault" and event.payload.event_type == "cert_expiring"
      and event.payload.cert_path starts_with "pki/digital-banking-platform"
    action:
      run_job_template:
        name: cert-rotation-vault-pki
        organization: UHCCP
        job_args:
          extra_vars:
            pki_mount: pki/digital-banking-platform
            application: digital-banking-platform
