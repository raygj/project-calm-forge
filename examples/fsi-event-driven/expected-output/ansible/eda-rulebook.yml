# ============================================================
# GENERATED BY: CALM Forge v0.2
# EDA rulebook for trade-pipeline
# DO NOT EDIT — regenerate from CALM source
# ============================================================
- name: trade-pipeline Day 2 Operations
  hosts: all
  sources:
  - ansible.eda.webhook:
      host: 0.0.0.0
      port: 5000
  rules:
  - name: order-intake config drift remediation
    condition: event.payload.alert_type == "configuration_drift" and event.payload.entity_id
      == "order-intake"
    action:
      run_job_template:
        name: day2-drift-remediation
        organization: UHCCP
        job_args:
          extra_vars:
            target_service: order-intake
            application: trade-pipeline
  - name: order-intake agent health restore
    condition: event.payload.alert_type == "agent_unhealthy" and event.payload.entity_id
      == "order-intake"
    action:
      run_job_template:
        name: day2-agent-restore
        organization: UHCCP
        job_args:
          extra_vars:
            target_service: order-intake
            application: trade-pipeline
  - name: trade-processor config drift remediation
    condition: event.payload.alert_type == "configuration_drift" and event.payload.entity_id
      == "trade-processor"
    action:
      run_job_template:
        name: day2-drift-remediation
        organization: UHCCP
        job_args:
          extra_vars:
            target_service: trade-processor
            application: trade-pipeline
  - name: trade-processor agent health restore
    condition: event.payload.alert_type == "agent_unhealthy" and event.payload.entity_id
      == "trade-processor"
    action:
      run_job_template:
        name: day2-agent-restore
        organization: UHCCP
        job_args:
          extra_vars:
            target_service: trade-processor
            application: trade-pipeline
  - name: settlement-engine config drift remediation
    condition: event.payload.alert_type == "configuration_drift" and event.payload.entity_id
      == "settlement-engine"
    action:
      run_job_template:
        name: day2-drift-remediation
        organization: UHCCP
        job_args:
          extra_vars:
            target_service: settlement-engine
            application: trade-pipeline
  - name: settlement-engine agent health restore
    condition: event.payload.alert_type == "agent_unhealthy" and event.payload.entity_id
      == "settlement-engine"
    action:
      run_job_template:
        name: day2-agent-restore
        organization: UHCCP
        job_args:
          extra_vars:
            target_service: settlement-engine
            application: trade-pipeline
  - name: audit-logger config drift remediation
    condition: event.payload.alert_type == "configuration_drift" and event.payload.entity_id
      == "audit-logger"
    action:
      run_job_template:
        name: day2-drift-remediation
        organization: UHCCP
        job_args:
          extra_vars:
            target_service: audit-logger
            application: trade-pipeline
  - name: audit-logger agent health restore
    condition: event.payload.alert_type == "agent_unhealthy" and event.payload.entity_id
      == "audit-logger"
    action:
      run_job_template:
        name: day2-agent-restore
        organization: UHCCP
        job_args:
          extra_vars:
            target_service: audit-logger
            application: trade-pipeline
  - name: postgres-db connection pool monitoring
    condition: event.payload.alert_type == "connection_pool_exhaustion" and event.payload.entity_id
      == "postgres-db"
    action:
      run_job_template:
        name: day2-db-connection-remediation
        organization: UHCCP
        job_args:
          extra_vars:
            target_database: postgres-db
            application: trade-pipeline
  - name: Credential rotation monitoring (trade-processor → postgres-db)
    condition: event.payload.source == "vault" and event.payload.event_type == "lease_expiring"
      and event.payload.mount_path == "database/trade-pipeline"
    action:
      run_job_template:
        name: credential-rotation-monitor
        organization: UHCCP
        job_args:
          extra_vars:
            db_mount: database/trade-pipeline
            consumer_service: trade-processor
            application: trade-pipeline
  - name: Credential rotation monitoring (settlement-engine → postgres-db)
    condition: event.payload.source == "vault" and event.payload.event_type == "lease_expiring"
      and event.payload.mount_path == "database/trade-pipeline"
    action:
      run_job_template:
        name: credential-rotation-monitor
        organization: UHCCP
        job_args:
          extra_vars:
            db_mount: database/trade-pipeline
            consumer_service: settlement-engine
            application: trade-pipeline
