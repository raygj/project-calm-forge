# ============================================================
# GENERATED BY: CALM Forge v0.2
# EDA rulebook for portfolio-platform
# DO NOT EDIT — regenerate from CALM source
# ============================================================
- name: portfolio-platform Day 2 Operations
  hosts: all
  sources:
  - ansible.eda.webhook:
      host: 0.0.0.0
      port: 5000
  rules:
  - name: portfolio-api-useast config drift remediation
    condition: event.payload.alert_type == "configuration_drift" and event.payload.entity_id
      == "portfolio-api-useast"
    action:
      run_job_template:
        name: day2-drift-remediation
        organization: UHCCP
        job_args:
          extra_vars:
            target_service: portfolio-api-useast
            application: portfolio-platform
  - name: portfolio-api-useast agent health restore
    condition: event.payload.alert_type == "agent_unhealthy" and event.payload.entity_id
      == "portfolio-api-useast"
    action:
      run_job_template:
        name: day2-agent-restore
        organization: UHCCP
        job_args:
          extra_vars:
            target_service: portfolio-api-useast
            application: portfolio-platform
  - name: portfolio-api-uswest config drift remediation
    condition: event.payload.alert_type == "configuration_drift" and event.payload.entity_id
      == "portfolio-api-uswest"
    action:
      run_job_template:
        name: day2-drift-remediation
        organization: UHCCP
        job_args:
          extra_vars:
            target_service: portfolio-api-uswest
            application: portfolio-platform
  - name: portfolio-api-uswest agent health restore
    condition: event.payload.alert_type == "agent_unhealthy" and event.payload.entity_id
      == "portfolio-api-uswest"
    action:
      run_job_template:
        name: day2-agent-restore
        organization: UHCCP
        job_args:
          extra_vars:
            target_service: portfolio-api-uswest
            application: portfolio-platform
  - name: portfolio-api-euwest config drift remediation
    condition: event.payload.alert_type == "configuration_drift" and event.payload.entity_id
      == "portfolio-api-euwest"
    action:
      run_job_template:
        name: day2-drift-remediation
        organization: UHCCP
        job_args:
          extra_vars:
            target_service: portfolio-api-euwest
            application: portfolio-platform
  - name: portfolio-api-euwest agent health restore
    condition: event.payload.alert_type == "agent_unhealthy" and event.payload.entity_id
      == "portfolio-api-euwest"
    action:
      run_job_template:
        name: day2-agent-restore
        organization: UHCCP
        job_args:
          extra_vars:
            target_service: portfolio-api-euwest
            application: portfolio-platform
  - name: mongodb-primary connection pool monitoring
    condition: event.payload.alert_type == "connection_pool_exhaustion" and event.payload.entity_id
      == "mongodb-primary"
    action:
      run_job_template:
        name: day2-db-connection-remediation
        organization: UHCCP
        job_args:
          extra_vars:
            target_database: mongodb-primary
            application: portfolio-platform
  - name: mongodb-secondary-uswest connection pool monitoring
    condition: event.payload.alert_type == "connection_pool_exhaustion" and event.payload.entity_id
      == "mongodb-secondary-uswest"
    action:
      run_job_template:
        name: day2-db-connection-remediation
        organization: UHCCP
        job_args:
          extra_vars:
            target_database: mongodb-secondary-uswest
            application: portfolio-platform
  - name: mongodb-secondary-euwest connection pool monitoring
    condition: event.payload.alert_type == "connection_pool_exhaustion" and event.payload.entity_id
      == "mongodb-secondary-euwest"
    action:
      run_job_template:
        name: day2-db-connection-remediation
        organization: UHCCP
        job_args:
          extra_vars:
            target_database: mongodb-secondary-euwest
            application: portfolio-platform
  - name: global-load-balancer config drift remediation
    condition: event.payload.alert_type == "configuration_drift" and event.payload.entity_id
      == "global-load-balancer"
    action:
      run_job_template:
        name: day2-drift-remediation
        organization: UHCCP
        job_args:
          extra_vars:
            target_service: global-load-balancer
            application: portfolio-platform
  - name: global-load-balancer agent health restore
    condition: event.payload.alert_type == "agent_unhealthy" and event.payload.entity_id
      == "global-load-balancer"
    action:
      run_job_template:
        name: day2-agent-restore
        organization: UHCCP
        job_args:
          extra_vars:
            target_service: global-load-balancer
            application: portfolio-platform
  - name: Certificate auto-renewal (global-load-balancer ↔ portfolio-api-useast mTLS)
    condition: event.payload.source == "vault" and event.payload.event_type == "cert_expiring"
      and event.payload.cert_path starts_with "pki/portfolio-platform"
    action:
      run_job_template:
        name: cert-rotation-vault-pki
        organization: UHCCP
        job_args:
          extra_vars:
            pki_mount: pki/portfolio-platform
            application: portfolio-platform
  - name: Certificate auto-renewal (global-load-balancer ↔ portfolio-api-uswest mTLS)
    condition: event.payload.source == "vault" and event.payload.event_type == "cert_expiring"
      and event.payload.cert_path starts_with "pki/portfolio-platform"
    action:
      run_job_template:
        name: cert-rotation-vault-pki
        organization: UHCCP
        job_args:
          extra_vars:
            pki_mount: pki/portfolio-platform
            application: portfolio-platform
  - name: Certificate auto-renewal (global-load-balancer ↔ portfolio-api-euwest mTLS)
    condition: event.payload.source == "vault" and event.payload.event_type == "cert_expiring"
      and event.payload.cert_path starts_with "pki/portfolio-platform"
    action:
      run_job_template:
        name: cert-rotation-vault-pki
        organization: UHCCP
        job_args:
          extra_vars:
            pki_mount: pki/portfolio-platform
            application: portfolio-platform
  - name: Certificate auto-renewal (mongodb-primary ↔ mongodb-secondary-uswest mTLS)
    condition: event.payload.source == "vault" and event.payload.event_type == "cert_expiring"
      and event.payload.cert_path starts_with "pki/portfolio-platform"
    action:
      run_job_template:
        name: cert-rotation-vault-pki
        organization: UHCCP
        job_args:
          extra_vars:
            pki_mount: pki/portfolio-platform
            application: portfolio-platform
  - name: Certificate auto-renewal (mongodb-primary ↔ mongodb-secondary-euwest mTLS)
    condition: event.payload.source == "vault" and event.payload.event_type == "cert_expiring"
      and event.payload.cert_path starts_with "pki/portfolio-platform"
    action:
      run_job_template:
        name: cert-rotation-vault-pki
        organization: UHCCP
        job_args:
          extra_vars:
            pki_mount: pki/portfolio-platform
            application: portfolio-platform
