# ============================================================
# GENERATED BY: CALM Forge v0.2
# EDA rulebook for fraud-detection-pipeline
# DO NOT EDIT — regenerate from CALM source
# ============================================================
- name: fraud-detection-pipeline Day 2 Operations
  hosts: all
  sources:
  - ansible.eda.webhook:
      host: 0.0.0.0
      port: 5000
  rules:
  - name: transaction-ingest config drift remediation
    condition: event.payload.alert_type == "configuration_drift" and event.payload.entity_id
      == "transaction-ingest"
    action:
      run_job_template:
        name: day2-drift-remediation
        organization: UHCCP
        job_args:
          extra_vars:
            target_service: transaction-ingest
            application: fraud-detection-pipeline
  - name: transaction-ingest agent health restore
    condition: event.payload.alert_type == "agent_unhealthy" and event.payload.entity_id
      == "transaction-ingest"
    action:
      run_job_template:
        name: day2-agent-restore
        organization: UHCCP
        job_args:
          extra_vars:
            target_service: transaction-ingest
            application: fraud-detection-pipeline
  - name: ml-scoring config drift remediation
    condition: event.payload.alert_type == "configuration_drift" and event.payload.entity_id
      == "ml-scoring"
    action:
      run_job_template:
        name: day2-drift-remediation
        organization: UHCCP
        job_args:
          extra_vars:
            target_service: ml-scoring
            application: fraud-detection-pipeline
  - name: ml-scoring agent health restore
    condition: event.payload.alert_type == "agent_unhealthy" and event.payload.entity_id
      == "ml-scoring"
    action:
      run_job_template:
        name: day2-agent-restore
        organization: UHCCP
        job_args:
          extra_vars:
            target_service: ml-scoring
            application: fraud-detection-pipeline
  - name: external-state config drift remediation
    condition: event.payload.alert_type == "configuration_drift" and event.payload.entity_id
      == "external-state"
    action:
      run_job_template:
        name: day2-drift-remediation
        organization: UHCCP
        job_args:
          extra_vars:
            target_service: external-state
            application: fraud-detection-pipeline
  - name: external-state agent health restore
    condition: event.payload.alert_type == "agent_unhealthy" and event.payload.entity_id
      == "external-state"
    action:
      run_job_template:
        name: day2-agent-restore
        organization: UHCCP
        job_args:
          extra_vars:
            target_service: external-state
            application: fraud-detection-pipeline
  - name: decision-emitter config drift remediation
    condition: event.payload.alert_type == "configuration_drift" and event.payload.entity_id
      == "decision-emitter"
    action:
      run_job_template:
        name: day2-drift-remediation
        organization: UHCCP
        job_args:
          extra_vars:
            target_service: decision-emitter
            application: fraud-detection-pipeline
  - name: decision-emitter agent health restore
    condition: event.payload.alert_type == "agent_unhealthy" and event.payload.entity_id
      == "decision-emitter"
    action:
      run_job_template:
        name: day2-agent-restore
        organization: UHCCP
        job_args:
          extra_vars:
            target_service: decision-emitter
            application: fraud-detection-pipeline
  - name: Certificate auto-renewal (decision-emitter ↔ payment-gateway mTLS)
    condition: event.payload.source == "vault" and event.payload.event_type == "cert_expiring"
      and event.payload.cert_path starts_with "pki/fraud-detection-pipeline"
    action:
      run_job_template:
        name: cert-rotation-vault-pki
        organization: UHCCP
        job_args:
          extra_vars:
            pki_mount: pki/fraud-detection-pipeline
            application: fraud-detection-pipeline
