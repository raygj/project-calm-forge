# ============================================================
# GENERATED BY: CALM Forge v0.2
# EDA rulebook for payments-portal
# DO NOT EDIT — regenerate from CALM source
# ============================================================
- name: payments-portal Day 2 Operations
  hosts: all
  sources:
  - ansible.eda.webhook:
      host: 0.0.0.0
      port: 5000
  rules:
  - name: web-frontend config drift remediation
    condition: event.payload.alert_type == "configuration_drift" and event.payload.entity_id
      == "web-frontend"
    action:
      run_job_template:
        name: day2-drift-remediation
        organization: UHCCP
        job_args:
          extra_vars:
            target_service: web-frontend
            application: payments-portal
  - name: web-frontend agent health restore
    condition: event.payload.alert_type == "agent_unhealthy" and event.payload.entity_id
      == "web-frontend"
    action:
      run_job_template:
        name: day2-agent-restore
        organization: UHCCP
        job_args:
          extra_vars:
            target_service: web-frontend
            application: payments-portal
  - name: api-service config drift remediation
    condition: event.payload.alert_type == "configuration_drift" and event.payload.entity_id
      == "api-service"
    action:
      run_job_template:
        name: day2-drift-remediation
        organization: UHCCP
        job_args:
          extra_vars:
            target_service: api-service
            application: payments-portal
  - name: api-service agent health restore
    condition: event.payload.alert_type == "agent_unhealthy" and event.payload.entity_id
      == "api-service"
    action:
      run_job_template:
        name: day2-agent-restore
        organization: UHCCP
        job_args:
          extra_vars:
            target_service: api-service
            application: payments-portal
  - name: database connection pool monitoring
    condition: event.payload.alert_type == "connection_pool_exhaustion" and event.payload.entity_id
      == "database"
    action:
      run_job_template:
        name: day2-db-connection-remediation
        organization: UHCCP
        job_args:
          extra_vars:
            target_database: database
            application: payments-portal
  - name: Certificate auto-renewal (web-frontend ↔ api-service mTLS)
    condition: event.payload.source == "vault" and event.payload.event_type == "cert_expiring"
      and event.payload.cert_path starts_with "pki/payments-portal"
    action:
      run_job_template:
        name: cert-rotation-vault-pki
        organization: UHCCP
        job_args:
          extra_vars:
            pki_mount: pki/payments-portal
            application: payments-portal
  - name: Credential rotation monitoring (api-service → database)
    condition: event.payload.source == "vault" and event.payload.event_type == "lease_expiring"
      and event.payload.mount_path == "database/payments-portal"
    action:
      run_job_template:
        name: credential-rotation-monitor
        organization: UHCCP
        job_args:
          extra_vars:
            db_mount: database/payments-portal
            consumer_service: api-service
            application: payments-portal
